This files in this directory should be copied into /etc/nginx/
Replace the paths to the certificates and domain names accordingly.

#1 Replace the nginx.conf by the nginx-root.conf
sudo mv ./nginx-root.conf /etc/nginx/nginx.conf
sudo mv smartscope*.conf /etc/nginx